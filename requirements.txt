flask==2.0.3  # Compatible with Python 3.8
werkzeug==2.0.3  # Pinned — newer werkzeug removes internals flask==2.0.3 depends on
flask-socketio==5.1.2  # For WebSocket support
neo4j==4.4.12  # Graph DB driver, optional — app runs fine without it
jsonrpcserver==3.5.6  # Last stable release before major API changes; patched at
                      # build time for Python 3.10+ (uses removed collections ABCs)
jsonrpcclient==4.0.3
pandas>=2.0,<2.3  # Bumped from 1.3.5 — no wheel exists for Python 3.10+; verified
                  # no removed-API usage (.append/.iteritems/.ix) in this codebase
requests==2.28.2  # For HTTP clients
websocket-client==1.3.3  # For WebSocket clients
python-dotenv==0.21.1  # For .env file handling
